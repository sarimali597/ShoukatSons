<UserControl
  x:Class="ShoukatSons.POS.Wpf.Views.BarcodePrint.BarcodePrintView"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  mc:Ignorable="d"
  d:DesignHeight="450"
  d:DesignWidth="800"
  Background="#0F1A33">

  <Grid Margin="20">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <DockPanel Grid.Row="0" LastChildFill="True" Margin="0,0,0,12">
      <TextBlock
        Text="{Binding Header}"
        FontSize="22"
        FontWeight="SemiBold"
        Foreground="White"
        VerticalAlignment="Center"/>
      <Button
        Content="Exit to Dashboard"
        Command="{Binding ExitCommand}"
        HorizontalAlignment="Right"
        Margin="12,0,0,0"
        Padding="12,6"/>
    </DockPanel>

    <Border
      Grid.Row="1"
      CornerRadius="8"
      BorderBrush="#1F2B4D"
      BorderThickness="1"
      Background="#162447"
      Padding="16">
      <TextBlock
        Text="{Binding Info}"
        Foreground="#BFD1FF"
        FontSize="14"/>
    </Border>
  </Grid>
</UserControl>