<Window x:Class="ShoukatSons.UI.Views.POSWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Point of Sale" Height="600" Width="900"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top: Barcode Entry -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
            <TextBox Width="220"
                     Margin="0,0,10,0"
                     Text="{Binding Barcode, UpdateSourceTrigger=PropertyChanged}">
                <TextBox.InputBindings>
                    <KeyBinding Key="Enter" Command="{Binding AddItemCommand}" />
                </TextBox.InputBindings>
            </TextBox>
            <Button Content="Add" Width="100" Command="{Binding AddItemCommand}"/>
        </StackPanel>

        <!-- Middle: Cart Items -->
        <DataGrid Grid.Row="1"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  HeadersVisibility="Column"
                  IsReadOnly="True"
                  ItemsSource="{Binding CartItems}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Barcode" Binding="{Binding Barcode}" Width="*"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="2*"/>
                <DataGridTextColumn Header="Qty" Binding="{Binding Quantity}" Width="Auto">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Price" Width="Auto"
                                    Binding="{Binding Price, StringFormat=N2}"/>
                <DataGridTextColumn Header="Total" Width="Auto"
                                    Binding="{Binding Total, StringFormat=N2}"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Bottom: Total & Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <TextBlock Text="Total: " FontSize="18" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding Total, StringFormat=N2}"
                       FontSize="18" FontWeight="Bold" VerticalAlignment="Center" Margin="5,0,20,0"/>
            <Button Content="Checkout" Width="120" Command="{Binding CheckoutCommand}" Margin="0,0,5,0"/>
            <Button Content="Close" Width="80" Command="{Binding CloseCommand}"/>
        </StackPanel>
    </Grid>
</Window>